# For loops demonstrated — Module 2 Lesson 2 core mastered

The learner demonstrated genuine understanding of Python for loops: replacing three manual indexed lines with a single loop; predicting the loop variable's value across passes and its persistence after the loop (no block scope — contrasted with JS `const` in `for...of`); predicting the un-indented-print trap (one call after the loop, holding the last prompt); writing a multi-statement loop body; building the accumulator pattern (empty list + `append`); measuring results with `len()` instead of guessing; and recovering the prompt → response → append flow after briefly storing prompts instead of responses (recognized the "looks right, secretly wrong" failure mode). Working method established: code written into `practice\m2l2-for-loops.py` with stubbed course helpers, chat reserved for non-code reasoning.

**Implications**: The for loop is the tool that makes the M2L1 task-list pattern scale to any list length. JS-analog traps to keep flagging: `.forEach`/`.map` are not methods on Python lists, the loop variable is the value not the index, indentation is membership, and prompt ≠ response. Next lesson (M2L3) opens the dictionaries hook Andrew left at the end of the video — finding items by name instead of position.