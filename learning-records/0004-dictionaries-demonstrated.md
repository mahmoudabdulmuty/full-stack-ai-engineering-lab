# Dictionaries demonstrated — Module 2 Lesson 3 core mastered

The learner demonstrated genuine understanding of Python dictionaries: creating them with curly braces and `key: value` syntax; accessing values by key with square brackets; triggering and recognizing `KeyError` for missing keys vs JS `undefined`; using `.get()` for safe access with `None` default and custom defaults; membership testing with `in` operator; adding new keys and updating existing ones with the identical `dict[key] = value` pattern; storing mixed-type values (int, string, list) in one dict; building a priority dictionary whose values are lists and looping over `prioritized_tasks["high priority"]` with `print_llm_response` — composing the M2L2 loop pattern with M2L3 dict access. JS-object analogy used throughout; traps flagged: no dot access, no index positions, KeyError vs undefined, .get() as the safe pattern. Working method maintained: all code written into `practice\m2l3-dictionaries.py` with stubbed helpers, chat reserved for non-code reasoning.

**Implications**: The "find by name" pain from M2L2 is fully resolved. Dictionary + for loop composition is the M2L2+M2L3 combined pattern. Next lesson (M2L4) uses dict VALUES to customize LLM prompts — passing description strings into f-strings. Vocabulary for dict now earned (glossary entry added).