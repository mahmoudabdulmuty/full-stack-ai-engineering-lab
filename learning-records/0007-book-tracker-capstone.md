# Book Tracker capstone demonstrated — Module 2 project passed

The learner completed the local rebuild of the M2 graded assignment (Book Tracker) across three iterations, demonstrating and then FIXING the highest-value bugs of the module so far: (1) JS shorthand-property instinct `{title, author}` → discovered Python has no shorthand and `{title: title}` uses the variable's VALUE as a key — fixed to explicit string keys; (2) `{a, b, c}` set-vs-dict disambiguation after hitting `'set' object is not subscriptable`; (3) `.toLowerCase()` JS method leak → `.lower()`; (4) early-return-inside-loop killing accumulation + silent None drops → refactored to collect-all-then-return-the-list with call-site prints; (5) dead code after return removed. Final version: all 5 parts pass (add_book with validation + return len; formatted shelf loop; case-insensitive find_books returning a list; if/else bedtime filter with accumulator; multi-line f-string prompt interpolating profile dict AND full database via a builder function). Also asked about guard clauses/early returns — pattern validated, placement distinction (before loop vs inside loop) taught. Noted learner intuitions to revisit later: keyword arguments, dicts-as-params, type annotations (`: dict` — TS instinct), default parameters (missing-args TypeError question).

**Implications**: Module 2 fully closed at the project level. The set-vs-dict and return-semantics lessons were self-discovered through debugging — high storage strength expected. Next: interview-style hard test (no hints, trick questions, combined M1+M2 concepts), then Module 3 (files/documents — mission-critical, teach deep).
