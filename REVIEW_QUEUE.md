# REVIEW_QUEUE.md

Spaced retrieval schedule. Interval ladder per item: 1st review ~3 days → 2nd ~7 days → 3rd ~21–30 days → then fold into project work. Each review must use a genuinely new context/task, not renamed variables.

Rule: each session opens with 0–1 due item as a 5–10 min warm-up BEFORE new material. Learner answers cold; tutor grades honestly; item only leaves the queue after passing on schedule.

Rechecks should progressively increase transfer distance: unfamiliar domain or dataset → incomplete existing program → interacting concepts → deliberately inserted bug/edge case → ambiguous requirement where the learner chooses the relevant concepts. Do not reveal the bug category or failing line once debugging independence is being tested. Record meaningful hints/repair assistance so Guided is never mistaken for Independent.

Keep retrieval lean: schedule meaningful or fragile capabilities, not every syntax detail. Require explicit prediction mainly when the mental model, state transition, failure, or integration is under test. Fluency observations (start independence, hint count, trial-and-error, explanation clarity, retrieval effort) are qualitative and separate from correctness.

## Pending first reviews (seeded 2026-08-27)

Module 4 Lesson 1 debrief closed on 2026-09-09. Select 0–1 eligible review per session; dates indicate eligibility, not mandatory extra sessions. For ties, choose the item relevant to current work, otherwise use table order. New-context tasks below remain delivery drafts.

| Item | Origin | Status | Warm-up task (new context) | Last result | Next due |
|---|---|---|---|---|---|
| f-string freeze timing | 0005 | Demonstrated | Assign f-string to var, change source vars, print var — predict before run; explain what was frozen at assignment. | No cold result recorded | Eligible since 2026-09-08 |
| Return semantics + append/remove-return-None | 0002/M1 | Demonstrated | One function where the bug is `list.append(...)` assigned to a var and returned; trace output aloud. | No cold result recorded | Eligible since 2026-09-08 |
| Set-vs-dict literals + string keys | 0007 | Demonstrated | Predict TypeError type and message for `{name}` followed by `[0]`; contrast working dict literal. | No cold result recorded | Eligible since 2026-09-08 |
| Guard clauses & early returns placement | 0007 | Demonstrated | Two near-identical loops differing only in check placement inside vs before loop; explain outputs. | No cold result recorded | Eligible since 2026-09-08 |

Scheduling clarification — 2026-09-08: a guided retry does not advance the cold-review ladder or erase an earlier cold result. Record both separately; make a retry eligible about three days after the latest guided work, or at its named project milestone if later. On a cold pass, schedule the next ladder interval from that pass. The existing task cap still applies. Historical completed-review wording below is preserved: references to mutator returns mean the covered `append()`/`remove()` operations, and historical classifier correctness means substring/stub behavior only (see the tutor correction in `learning-records/0012-module-3-assessment-guided.md`).

## Scheduled (later ladder steps)

| Item | Current step | Scheduled context idea |
|---|---|---|
| Dict safe access (`KeyError` vs `.get()` vs `in`) | first cold recheck passed 2026-09-04 | Recheck around 2026-09-11 in a changed API/config-data context; require the learner to choose whether absence is exceptional or expected. |
| Comparisons / if-else nesting / bool operators | awaiting first cold recheck | At the first real automation feature needing compound filtering; avoid a disconnected syntax drill. |
| JS→Python method translation | recurring watchlist | At the first real automation implementation: given JS snippet, produce Python equivalent cold (`.toLowerCase`, `.trim`, ternary, spread vs `*unpack`) |
| CWD-relative paths (Demonstrated 2026-08-27; Module 3 Q5 guided recheck 2026-09-05) | exact path-resolution model remains fragile | Eligible since 2026-09-08 (last guided attempt: 2026-09-05; last recorded self-recovery: 2026-08-27). Check from a third directory: learner writes the exact resolved path before running and repairs it without being reminded that CWD is prepended. |
| `in`-on-strings case choice (weak-spot #4; still Demonstrated 2026-08-27 — M3L3 C4 demonstrated substring/stub behavior, not a valid label classifier) | first formal review | In the first real-provider normalization/filtering context, revisit case normalization and the tutor-identified label collision (`relevant` is also in `not relevant`); this is a planned review, not new learner evidence. |
| File mode + handle lifecycle (Demonstrated practical flow 2026-08-28; Module 3 Q6 guided recheck 2026-09-05) | Recalled that `"w"` empties the file, but exact truncation timing and `io.UnsupportedOperation` wrong-mode vs `ValueError` closed-handle distinction required direct teaching. | Eligible since 2026-09-08; last cold truncation pass: 2026-08-28, last guided attempt: 2026-09-05. Mixed-state check without named categories: infer mode, open/closed state, and exception from symptoms. |
| Stale variables across consecutive loops (M3L4 C3) | targeted re-probe | At the already planned cold Module 3 project: use two loops over different datasets where the second loop accidentally interpolates the first loop's variables; learner traces output and repairs without naming the stale variables. |
| `csv.DictReader` input + context-manager lifetime (M3L5; Q8 guided rebuild 2026-09-05) | Q9 immediate guided application completed; live-handle repair and materialized-list output verified, but lazy-reader lifetime required extensive explanation and experiment | At the already planned cold Module 3 project (last guided attempt: 2026-09-05; no cold lifetime pass recorded): learner chooses handle vs content, returns materialized data when needed beyond the context, and explains resource ownership without prompts. |
| Parameterized reusable function + `return`/caller boundary (M3L6 Guided 2026-09-04) | Q9 applied reusable readers under a staged scaffold; first unguided changed-context implementation remains due | In the Phase 1 real automation, require repeated processing without a function scaffold. Learner chooses parameters and return values, keeps presentation in the caller, and explains the boundary. |
| Input list vs result accumulator + dynamic dictionary keys (M3L7 Guided 2026-09-04) | 2026-09-04 cold core implementation passed; key-only output needed one hint; 2026-09-05 Q9 application was Guided; spaced retention check next | Eligible 2026-09-11, change both domain and surrounding control flow; require a separate accumulator and dynamic keys without naming either pattern. |
| String output accumulation across multiple mapping entries (Module 3 Q9 Guided 2026-09-05) | 2026-08-27 basic accumulator cold pass; latest string accumulation attempt: Guided 2026-09-05, final output retained both tickets after a focused repair from `=` to `+=`; newline separation also needed a hint | At the already planned cold Module 3 project: aggregate several dynamically keyed responses into one string or report without naming the accumulator pattern or separator requirement. Do not add an immediate parallel drill; Q9 was already the focused practice session. |
| Deterministic orchestration vs real-LLM behavior (Module 3 Q10 Guided 2026-09-05) | correctly identified the echo limitation and deterministic file ownership; needed two focused Part 1 retries and one Part 3 rationale retry | Fold into the first real-LLM integration: learner marks which guarantees are Python-testable, which require output validation, and which require probabilistic evaluation, without named categories. |
| Local module alias binding (M4L1 Guided 2026-09-09) | selected the correct alias-qualified call, but two explanation attempts reversed whether the alias or original module name was bound | Eligible around 2026-09-12. Use a different module/package context and require the learner to predict the bound names and valid call without a syntax reminder. |

## Completed reviews

| Date | Item | Result |
|---|---|---|
| 2026-08-27 | Accumulator pattern (review queue seeded item) | Cold, no hints. D1 ran clean; output `2` matched prediction on first try. Confirmed init-before-loop; list accumulator survives loop exit. Single residual: Drill D5 predicted "2" but actual `"a" in item.lower()` matched 3 — learner forgot the check runs on the lowered copy. Moved to weak-spot #4 + scheduled review row; Drill 6 (the "the"-files task) left as next warm-up. |
| 2026-08-27 | CWD-relative paths (re-probe during M3L3 C1) | Self-recovered: ran from `practice\data` with `open("cape-town.txt", ...)`; reflected, switched paths, articulated the rule (terminal CWD = where Python looks; open() path is relative to terminal, not script). Moved to Demonstrated. |
| 2026-08-27 | `in`-on-strings case choice (M3L3 C4) | Code correct: `if "relevant" in response:`. Predicted 2 files; actual 3 (stub echoes prompt). Learner explained cause after run (stub includes the word in its own instructions). Capability Demonstrated, predict-grounding lesson captured. |
| 2026-08-28 | File truncation timing (M3L5 cold opener) | Correctly predicted that opening an existing file with `"w"` empties it immediately, so a crash before `.write()` leaves an empty file. Passed cold without hints. |
| 2026-09-04 | Basic `return` versus `print` transfer check (M3L6 debrief) | Given a changed player-report function that printed a slice but had no `return`, correctly predicted the caller variable contains `None` and explained why. This checks basic return semantics only; the broader mutator-return-`None` item remains queued. |
| 2026-09-04 | Literal versus dynamic dictionary key, immediate transfer (M3L7 debrief) | In a ticket-summary loop, correctly predicted that literal key `"id"` retains only the last response and proposed `ticket["id"]` so both IDs become keys. Same-session guided transfer, not cold evidence. |
| 2026-09-04 | Missing dictionary key vs safe access | Cold player-stat context after the M3L5 miss: correctly predicted `player["goals"]` raises `KeyError`, self-corrected the membership key to `"goals" in player` → `False`, and supplied `player.get("goals", 0)`. First spaced check passed; recheck later for retention. |
| 2026-09-04 | Input collection vs result accumulator + dynamic dictionary keys | Cold ticket-domain implementation used a separate `result` dictionary, derived changing `T-101` / `T-102` keys from each row, made one stub call per ticket, predicted their order, and verified `dict_keys(['T-101', 'T-102'])`. Core implementation was independent; one focused hint changed the final print from the whole dictionary to its keys. |
| 2026-09-05 | CWD-relative path resolution (Module 3 assessment Q5) | Correctly distinguished success from repository root and failure after `cd practice`, and repaired the usable path to `data/tokyo.txt`. Needed one hint plus direct explanation to state the exact duplicated resolution `practice/practice/data/tokyo.txt`; retain as fragile rather than promoting. |
| 2026-09-05 | File mode and handle state (Module 3 assessment Q6) | Knew the `"w"` result becomes empty and ultimately distinguished open-but-not-readable from readable-but-closed. Timing and exact exceptions required guidance: truncation at open, `io.UnsupportedOperation` for wrong mode, `ValueError` for a closed handle. Guided evidence; keep queued. |
| 2026-09-05 | Echo-stub substring trap (Module 3 assessment Q7) | Correctly predicted all three filenames because the stub echoes the prompt containing `"relevant"`; execution matched insertion order. Needed output-format and data-flow precision feedback, and a correction that a real model's classifications are not guaranteed. Core stub-trap reasoning passed cold. |
| 2026-09-05 | `csv.DictReader` input and lifetime (Module 3 assessment Q8) | Minimal handle repair and final `list[dict]` outputs were correct. A returned-reader experiment demonstrated representation versus consumption: `print(reader)` displayed an object, while `next(reader)` raised `ValueError` after `with` closed the file. Understanding required stepwise rebuilding, so capability remains fragile/Guided and moves directly into staged Q9 integration. |
| 2026-09-05 | Module 3 support-ticket integration (Q9) | Final run exited 0 and produced ordered keys `T-201`, `T-203`; the written/read-back report contained both echoed prompts. Reusable readers, deterministic filtering, detail-path derivation, dynamic keys, one stub call per selected ticket, and file verification all worked. Prediction and stub explanation were correct. Because the task was tutor-staged and prompt/accumulator repairs needed help, record it as Guided integration rather than an independent pass. |
| 2026-09-05 | Deterministic-vs-LLM boundary explanation (Q10) | After two focused Part 1 retries, named the three prompt inputs Python had already prepared. Correctly observed that the echo stub did not produce the required category/summary lines. After one focused Part 3 retry, explained that reading an unchanged file returns the same exact text without interpretation. Overall Guided; revisit at the first real model call. |
| 2026-09-09 | M4L1 local imports and alias debrief | Whole-module and direct-function imports ran clean with correct predictions. The changed-context alias call was correct, but the binding explanation needed a hint and then direct teaching after the second attempt. Recorded as Guided; cold alias review scheduled. |
